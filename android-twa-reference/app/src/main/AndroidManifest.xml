<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- La web usa cámara/galería (foto de perfil, fotos de listas, fotos y
         tickets en el chat) y micrófono (notas de voz) — sin estos dos
         permisos, Chrome no puede concedérselos a la web cuando los pida. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Para los avisos push (si más adelante se activan también en la
         versión de Play Store): -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Twa">

        <!-- Actividad que hace de "app nativa": abre tu web a pantalla
             completa usando Chrome por debajo. DEFAULT_URL es la dirección
             real de NoteUs — si algún día cambias de dominio, es lo único
             que hay que actualizar aquí. -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Twa"
            android:exported="true">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://lista-compartida-app.vercel.app/" />

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Esto es lo que hace que la app se abra SIN barra de
                 direcciones de Chrome (en vez de solo "parecer" una app) —
                 Android lo comprueba contra el assetlinks.json que tiene
                 que estar publicado en tu web (ver
                 ANDROID_PLAY_STORE_SETUP.md). Si el archivo no está bien
                 publicado, la app sigue funcionando pero se ve la barra de
                 Chrome arriba, como una web normal. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="lista-compartida-app.vercel.app" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <!-- Evita que se abra un Chrome "normal" (con su propia pestaña)
             cuando la web navega a otra URL del mismo dominio. -->
        <meta-data
            android:name="android.support.customtabs.trusted.LAUNCHER_NAME"
            android:value="@string/app_name" />
    </application>
</manifest>
